---
import { Image } from "@unpic/astro";

const { src, layout, width, height, alt, ...props } = Astro.props;
---

<Image
	src={src}
	layout={layout || "constrained"}
	width={width}
	height={height}
	alt={alt}
	{...props}
/>
